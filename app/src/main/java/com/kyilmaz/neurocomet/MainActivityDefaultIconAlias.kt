package com.kyilmaz.neurocomet

/**
 * Stable explicit entry point for IDE/debug launches that target
 * `MainActivityDefaultIconAlias` directly.
 *
 * The visible default app icon is provided by a separate launcher alias so this
 * activity can remain enabled even when icon switching disables the default icon
 * component.
 */
class MainActivityDefaultIconAlias : MainActivity()

