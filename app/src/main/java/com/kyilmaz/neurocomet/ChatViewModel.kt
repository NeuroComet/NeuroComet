package com.kyilmaz.neurocomet

/**
 * ChatViewModel is intentionally thin: all DM/conversation logic lives in
 * [FeedViewModel] to keep a single source of truth for mock data.
 *
 * This class exists as a placeholder for future separation of concerns when
 * the app connects to a real messaging backend (e.g., Supabase Realtime).
 */
class ChatViewModel {
    // Future: migrate conversation state from FeedViewModel here
}
